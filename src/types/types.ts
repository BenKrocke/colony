export type NeighborArray = Array<Array<number>>;

export type Indexes = {
    row: number,
    col: number
}

export type Pathfinding = {
    cost: number,
    distanceFromStart: number;
}

export type Img = {
    src: string,
    alt: string
}